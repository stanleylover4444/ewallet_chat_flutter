
import 'package:flutter/material.dart';

class MycloudPage extends StatelessWidget {
  const MycloudPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // appBar: CustomConTactsToolbar(), 
      body:  Center(
        child: Text("cloud"),
      ),
    );
  }
}
